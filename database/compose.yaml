services:
  postgres:
    container_name: kimveloper
    image: postgres:18
    env_file:
      - ../.env
    ports:
      - "5432:5432"
    volumes:
      - kimveloper_data:/var/lib/postgresql
    restart: unless-stopped

volumes:
  kimveloper_data: